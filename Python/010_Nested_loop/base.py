x = int(input("what symbol would you like to use"))
y = int(input("What would you like the width of the box"))
z = int(input("What is the height of the box"))

if(the symbol that you want to use?)
if(The width of the box)
if(the height of the box)

print(the box width will either be different or the same number.)

for(? = x, '' + ?, '' - '')
for(? = y, '' + ?, '' - '')
for(? = z, '' + ?, '' - '')

